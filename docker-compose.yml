services:
  openai-dummy-api:
    build: .
    ports:
      - "7000:7000"
    environment:
      - HOST=0.0.0.0
      - PORT=7000
      - DEBUG=false
      - LOG_LEVEL=info
    restart: unless-stopped
